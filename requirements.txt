# The sleeve talks to the executor over HTTP, to a chat model for proposals, and to an
# embedding model for the news memory. Nothing here needs to match the executor's own
# pinned set — this runs as its own process.

google-genai>=1.0.0      # hosted chat + embeddings (Ollama needs no client library)
massive>=2.8.0           # prices and news
chromadb>=1.5.0          # the news vector store
yfinance>=0.2.60         # futures prices, and the fallback for equities
requests>=2.32.0
python-dotenv>=1.0.0
pytest>=8.0.0
